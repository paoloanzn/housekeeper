Default to Bun.

## CLI

```bash
bun dev          # watch
bun start        # run
bun run build    # compile standalone binary to dist/housekeeper
```

Entry point is `index.ts` (shebang + package.json `bin`).
